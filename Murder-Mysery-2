-- OwnerHub MM2 - Full Integrated AutoFarm Script
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

-- Настройки по умолчанию
getgenv().AutoFarmSettings = {
    Enabled = false,
    TweenSpeed = 18,
    CollectCoins = true,
    AutoEquipKnife = true
}

-- Функция поиска монет с полной поддержкой структуры MM2
local function getNearestCoin()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    
    local rootPart = character.HumanoidRootPart
    local nearestCoin = nil
    local shortestDistance = math.huge

    -- Проверка стандартных путей спавна монет в игре
    local containers = {
        Workspace:FindFirstChild("CoinContainer"),
        Workspace:FindFirstChild("Coins"),
        Workspace:FindFirstChild("Map") and Workspace.Map:FindFirstChild("CoinContainer")
    }

    for _, container in ipairs(containers) do
        if container then
            for _, coin in ipairs(container:GetChildren()) do
                local touchPart = coin:FindFirstChild("TouchInterest") or coin:FindFirstChildWhichIsA("BasePart")
                if touchPart and coin:IsA("BasePart") then
                    local distance = (rootPart.Position - coin.Position).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        nearestCoin = coin
                    end
                end
            end
        end
    end
    
    return nearestCoin
end

-- Основной защищенный поток автофарма
task.spawn(function()
    while true do
        task.wait()
        if getgenv().AutoFarmSettings.Enabled then
            pcall(function()
                local character = LocalPlayer.Character
                if not character or not character:FindFirstChild("HumanoidRootPart") then return end
                local rootPart = character.HumanoidRootPart
                local humanoid = character:FindFirstChildOfClass("Humanoid")

                if humanoid and humanoid.Health > 0 then
                    local targetCoin = getNearestCoin()
                    if targetCoin then
                        local distance = (rootPart.Position - targetCoin.Position).Magnitude
                        local timeToTravel = distance / math.clamp(getgenv().AutoFarmSettings.TweenSpeed, 5, 30)
                        
                        local startTime = tick()
                        local startPos = rootPart.Position
                        
                        while tick() - startTime < timeToTravel do
                            if not getgenv().AutoFarmSettings.Enabled then break end
                            local alpha = (tick() - startTime) / timeToTravel
                            rootPart.CFrame = CFrame.new(startPos:Lerp(targetCoin.Position, alpha))
                            RunService.RenderStepped:Wait()
                        end
                        
                        if targetCoin and targetCoin.Parent and getgenv().AutoFarmSettings.Enabled then
                            rootPart.CFrame = CFrame.new(targetCoin.Position)
                            task.wait(0.05)
                        end
                    end
                end
            end)
        end
    end
end)

-- Загрузка и инициализация интерфейса WindUI
local success, WindUI = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/source.lua"))()
end)

if success and WindUI then
    local Window = WindUI:CreateWindow({
        Title = "OwnerHub | Murder Mystery 2",
        Icon = "rbxassetid://6031075931",
        Author = "Full Edition",
        Folder = "OwnerHubData",
        Size = UDim2.fromOffset(580, 460),
        Transparency = 0.1,
        Theme = "Dark",
        SideBarWidth = 160,
        HasOutline = true
    })

    local Tab = Window:Tab({
        Title = "Главная",
        Icon = "home"
    })

    Tab:Toggle({
        Title = "Автофарм монеток",
        Description = "Автоматический сбор монет на карте",
        Default = false,
        Callback = function(state)
            getgenv().AutoFarmSettings.Enabled = state
        end
    })

    Tab:Slider({
        Title = "Скорость фарма",
        Description = "Регулировка скорости передвижения к монетам",
        Default = 18,
        Min = 5,
        Max = 30,
        Step = 1,
        Callback = function(value)
            getgenv().AutoFarmSettings.TweenSpeed = value
        end
    })
    
    WindUI:Notify({
        Title = "OwnerHub Запущен!",
        Content = "Полный скрипт успешно загружен и готов к работе.",
        Duration = 3
    })
end
